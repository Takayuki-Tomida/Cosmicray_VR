using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HaikeiManager : MonoBehaviour
{
    public static int haikei_flag = 0;
    public static int haikei_coroutine_flag = 0;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
